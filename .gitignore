# Build outputs
bin/
obj/
*.user
*.suo
.vs/

# Node
node_modules/
dist/
dist-ssr/
# ...but ASP.NET client-side libraries live in wwwroot/lib/*/dist and MUST be committed,
# otherwise jQuery 404s and every page script dies with "$ is not defined".
!GearZone.Web/wwwroot/lib/**
.vite/
*.local
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# VS Code
.vscode/*
!.vscode/extensions.json

# Data Protection shared key ring (not secrets to commit)
shared-keys/

# Env / secrets
*.env
appsettings.*.json
!appsettings.json
!appsettings.Development.json

# OS
.DS_Store
Thumbs.db

# Rider / VS Code
.idea/
*.code-workspace
