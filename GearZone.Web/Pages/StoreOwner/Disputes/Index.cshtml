@page "/storeowner/disputes"
@model GearZone.Web.Pages.StoreOwner.Disputes.IndexModel
@{
    ViewData["Title"] = "Dispute Management";
    ViewData["PageHeader"] = "Dispute Management";
    ViewData["ActivePage"] = "Disputes";
    ViewData["Breadcrumb"] = new string[] { "Dashboard", "Disputes" };
    Layout = "_StoreOwnerLayout";
}

<div class="flex flex-col gap-6 relative pb-28 w-full max-w-[1440px] mx-auto">
    <!-- Filters & Actions -->
    <div class="bg-white rounded-xl shadow-card border border-slate-100 p-4">
        <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-4">
            <div class="flex-1 w-full lg:w-auto relative">
                <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-slate-400">search</span>
                <input type="text" placeholder="Search disputes by ID or order ID..." class="w-full lg:max-w-md pl-10 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-all placeholder:text-slate-400">
            </div>
            <div class="flex flex-wrap items-center gap-3 w-full lg:w-auto">
                <div class="relative w-full sm:w-auto">
                    <select class="w-full sm:w-auto pl-4 pr-10 py-2 bg-white border border-slate-200 rounded-lg text-sm text-slate-700 appearance-none focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary cursor-pointer hover:bg-slate-50 transition-colors">
                        <option value="">All Statuses</option>
                        <option value="open">Open</option>
                        <option value="resolved">Resolved</option>
                        <option value="closed">Closed</option>
                    </select>
                    <span class="material-symbols-outlined absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none text-lg">expand_more</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Data Table Card -->
    <div class="bg-white rounded-xl shadow-card border border-slate-100 overflow-hidden flex flex-col relative w-full">
        <div class="overflow-x-auto">
            <table class="w-full text-sm text-left whitespace-nowrap">
                <thead class="bg-slate-50">
                    <tr>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider">Dispute ID</th>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider">Order ID</th>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider">Opened On</th>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider">Reason</th>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider">Status</th>
                        <th class="px-6 py-4 font-semibold text-slate-600 text-xs uppercase tracking-wider text-right">Action</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    <tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">
                        <div class="flex flex-col items-center gap-3">
                            <span class="material-symbols-outlined text-4xl text-slate-300">gavel</span>
                            <span>No disputes pending. You are doing well!</span>
                        </div>
                    </td></tr>
                </tbody>
            </table>
        </div>
        <div class="p-4 border-t border-slate-100 bg-slate-50/50 flex flex-col sm:flex-row items-center justify-between gap-4">
            <span class="text-sm text-slate-500">Showing <strong>0</strong> to <strong>0</strong> of <strong>0</strong> disputes</span>
        </div>
    </div>
</div>

